{
    "name": "wasm-array-tests",
    "version": "0.1.0",
    "author": {
        "name": "Colin Gilgenbach",
        "email": "hexane@mit.edu"
    },
    "scripts": {
        "build": "wasm-pack build --target bundler --release",
        "pretest": "wasm-pack build --target nodejs --dev",
        "test": "jest"
    },
    "devDependencies": {
        "@jest/globals": "^29.7.0",
        "jest": "^29.7.0",
        "ts-jest": "^29.2.2",
        "ts-node": "^10.9.2"
    }
}
